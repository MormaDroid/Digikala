package mohsen.morma.digikala.data.remote.model.profile

enum class ProfileScreenState(val state : String) {

    PROFILE_STATE("ProfileScreen"),
    LOGIN_STATE("ProfileLogin"),
    REGISTRY_STATE("ProfileRegistry")

}